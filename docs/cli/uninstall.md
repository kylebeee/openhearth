---
summary: "CLI reference for `openhearth uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `openhearth uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
openhearth uninstall
openhearth uninstall --all --yes
openhearth uninstall --dry-run
```
